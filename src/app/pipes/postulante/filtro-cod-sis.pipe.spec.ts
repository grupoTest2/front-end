import { FiltroCodSisPipe } from './filtro-cod-sis.pipe';

describe('FiltroCodSisPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCodSisPipe();
    expect(pipe).toBeTruthy();
  });
});
