import { FiltroConvocatoriasPipe } from './filtro-convocatorias.pipe';

describe('FiltroConvocatoriasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroConvocatoriasPipe();
    expect(pipe).toBeTruthy();
  });
});
